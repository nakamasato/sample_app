<%= form_for @micropost do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :content, :rows => 3 %>
  </div>
  <div class="action">
    <%= f.submit "Submit" %>
  </div>
<% end %>
